apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: demo-voting-app
spec:
  selector:
    app: demo-voting-app
    tier: backend
    component: postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432