apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    app: demo-voting-app
spec:
  type: NodePort
  selector:
    app: demo-voting-app
    tier: frontend
    component: result
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30081
